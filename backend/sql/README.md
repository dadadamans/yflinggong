# SQL 目录说明

这个目录按当前项目文档和演示版后端字段整理，面向 PostgreSQL。

文件说明：

- `schema.sql`：建表脚本
- `seed.sql`：初始化演示数据

当前说明：

- 现有 Java 后端仍然使用 `DemoStateService` 做内存存储。
- 这里的 SQL 已经把当前任务、订单、绑定、留言、会话需要的数据结构落出来，方便下一步切到 PostgreSQL。
- 字段命名尽量和现有前后端返回字段保持一致，减少后续联调改动。
