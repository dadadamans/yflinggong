# 银发零工小程序开发文档

## 1. 项目定位

本项目为一个 20 天内完成的微信小程序展示版项目，目标不是正式商用运营，而是用于课程设计、比赛答辩或项目演示。

项目核心流程只保留：

1. 发单方发布任务。
2. 老人浏览任务并接单。
3. 生成订单并修改状态。
4. 子女绑定后查看老人接单情况并进行沟通。

说明：

1. 发单方包括雇主和子女。
2. 老人只负责接单，不负责发单。
3. 子女除绑定和沟通外，也可以替家庭发布任务需求。

本项目采用简化开发方案：

1. 后端使用 `JDK 11 + Spring Boot + PostgreSQL + Redis`。
2. 文件上传使用 `OSS`，仅用于头像、任务图片。
3. 前端使用 `微信小程序`。
4. 不做真实支付、真实提现，只做“模拟结算”。

---

## 2. 为什么要简化

你当前的条件是：

1. 只有不到 20 天时间。
2. 小程序主体是个人。
3. 项目只需要展示，不需要真实运营。
4. 用户量极小，不需要复杂架构。

所以项目必须以“能做完、能演示、老师能看懂”为第一目标，而不是做成完整商业平台。

---

## 3. 个人主体小程序建议

对于个人主体小程序，这个项目建议不要做以下功能：

1. 真实微信支付。
2. 真实提现到微信零钱或银行卡。
3. 复杂企业认证、劳务结算、保险等正式运营能力。

原因是这些能力通常与主体资质、商户能力、审核要求有关。对你这个展示项目来说，最稳妥的方案是：

1. 支付改为“模拟支付”。
2. 提现改为“模拟提现申请”或直接不做。
3. 钱包只展示演示数据，不接真实资金。

参考资料：

1. 微信开放文档首页：<https://developers.weixin.qq.com/miniprogram/dev/framework/>
2. 微信订阅消息官方文档：<https://developers.weixin.qq.com/miniprogram/dev/framework/open-ability/subscribe-message.html>
3. 关于个人主体限制的整理说明：<https://www.ifanr.com/minapp/900763>

说明：个人主体能力限制和审核规则可能变化，真正提交审核前还需要你在微信公众平台后台再次确认。

---

## 4. 项目最终范围

### 4.1 用户角色

本项目只保留 4 个角色：

1. 老人用户。
2. 雇主用户。
3. 子女用户。
4. 管理员。

### 4.2 核心模块

本项目只做 5 个模块。

#### 模块一：登录注册模块

功能：

1. 微信登录。
2. 用户选择身份：老人 / 雇主 / 子女。
3. 填写基础资料。

#### 模块二：任务模块

功能：

1. 雇主发布任务。
2. 子女发布任务。
3. 老人查看任务列表。
4. 老人查看任务详情。
5. 老人接单。

#### 模块三：订单模块

功能：

1. 接单后生成订单。
2. 订单状态展示。
3. 发单方点击“完成任务”。
4. 老人查看完成状态。

建议订单状态只保留：

1. 待接单。
2. 进行中。
3. 已完成。
4. 已取消。

#### 模块四：子女绑定模块

这是本项目的重要特色模块，可以保留。

功能：

1. 老人发起绑定子女。
2. 子女输入绑定码进行绑定。
3. 子女查看老人资料。
4. 子女查看老人当前接单情况。
5. 子女查看订单状态。
6. 子女通过留言功能与老人沟通。

#### 模块五：后台管理模块

后台只做简单管理：

1. 查看用户列表。
2. 查看任务列表。
3. 查看订单列表。
4. 查看子女绑定关系。

---

## 5. 技术方案

### 5.1 后端技术

| 技术 | 版本 |
|---|---|
| JDK | 11 |
| Spring Boot | 2.7.x |
| PostgreSQL | 14.x |
| Redis | 6.x |
| Maven | 3.8+ |
| OSS | 阿里云 OSS |

### 5.2 前端技术

| 技术 | 说明 |
|---|---|
| 微信小程序原生开发 | 主方案 |
| WXML + WXSS + JS | 页面开发 |
| Vant Weapp | 可选组件库 |

### 5.3 架构说明

本项目不需要微服务，直接做单体项目即可。

系统结构：

```text
微信小程序
   │
   ▼
Spring Boot 后端
   ├─ PostgreSQL
   ├─ Redis
   └─ OSS
```

---

## 6. 页面设计

### 6.1 老人端页面

1. 首页。
2. 任务列表页。
3. 任务详情页。
4. 我的订单页。
5. 个人中心页。
6. 子女绑定页。

### 6.2 雇主端页面

1. 首页。
2. 发布任务页。
3. 我的任务页。
4. 订单管理页。
5. 个人中心页。

### 6.3 子女端页面

1. 绑定老人页。
2. 发布任务页。
3. 老人资料查看页。
4. 老人订单查看页。
5. 留言沟通页。

### 6.4 适老化要求

因为项目面向老人，页面要简单：

1. 字体大。
2. 按钮大。
3. 操作步骤少。
4. 颜色对比明显。
5. 首页功能入口不要太多。

---

## 7. 数据库简化设计

为了赶工期，数据库建议只做 7 张核心表。

### 7.1 用户表 `sys_user`

保存所有用户基础信息。

主要字段：

1. `id`
2. `openid`
3. `nickname`
4. `avatar_url`
5. `mobile`
6. `role_type`
7. `created_at`

### 7.2 老人信息表 `elderly_profile`

主要字段：

1. `id`
2. `user_id`
3. `real_name`
4. `age`
5. `gender`
6. `health_desc`
7. `skill_tags`
8. `emergency_contact`
9. `emergency_mobile`

### 7.3 雇主信息表 `employer_profile`

主要字段：

1. `id`
2. `user_id`
3. `name`
4. `mobile`
5. `remark`

### 7.4 任务表 `task_info`

主要字段：

1. `id`
2. `publisher_user_id`
3. `publisher_role_type`
4. `task_type`
5. `title`
6. `content`
7. `address`
8. `start_time`
9. `salary`
10. `status`
11. `image_url`

说明：

1. `publisher_user_id` 表示任务发布人，可以是雇主，也可以是子女。
2. `publisher_role_type` 用于区分当前发布人身份，建议取值：`employer` / `child`。

### 7.5 订单表 `order_info`

主要字段：

1. `id`
2. `task_id`
3. `elderly_user_id`
4. `employer_user_id`
5. `publisher_role_type`
6. `status`
7. `create_time`
8. `finish_time`

说明：

1. 展示项目里可以继续保留 `employer_user_id` 这个字段名，但它在业务上应理解为“发单方用户 id”。
2. 如果你想在后端实现时命名更严谨，也可以改成 `publisher_user_id`。

### 7.6 子女绑定表 `child_bind`

主要字段：

1. `id`
2. `elderly_user_id`
3. `child_user_id`
4. `bind_code`
5. `bind_status`
6. `created_at`

### 7.7 留言表 `message_record`

用于老人和子女沟通。

主要字段：

1. `id`
2. `elderly_user_id`
3. `child_user_id`
4. `sender_user_id`
5. `content`
6. `created_at`

---

## 8. 子女绑定模块设计

这个模块建议做成你项目的亮点。

### 8.1 业务流程

#### 方案一：老人生成绑定码，子女输入绑定码

流程如下：

1. 老人登录后进入“子女绑定”页面。
2. 系统生成一个 6 位绑定码。
3. 子女登录后输入绑定码。
4. 系统校验成功后建立绑定关系。

这个方案最简单，最适合你当前项目。

### 8.2 子女可查看内容

绑定成功后，子女可以查看：

1. 老人姓名。
2. 老人技能标签。
3. 老人当前接单情况。
4. 老人订单状态。
5. 老人紧急联系人信息。

### 8.3 子女可执行操作

建议只做轻量功能，不要做复杂代操作。

子女端可保留：

1. 查看老人订单。
2. 给老人留言。
3. 查看任务是否已完成。
4. 替家庭发布任务需求。

不要做：

1. 代接单。
2. 代提现。
3. 修改老人实名信息。

### 8.4 沟通功能简化方案

不要做实时聊天，时间不够。

建议做“留言板”功能：

1. 子女可留言。
2. 老人可查看留言。
3. 老人也可以回复。

这样既能体现“沟通”，开发量也小很多。

---

## 9. 支付与提现处理建议

### 9.1 本项目建议

本项目不接真实支付，不接真实提现。

### 9.2 替代实现

你可以这样设计：

1. 发单方发布任务时填写报酬金额。
2. 老人完成任务后，订单状态改为“已完成”。
3. 系统显示“待结算”或“模拟已结算”。

如果你一定要保留展示效果，可以加两个按钮：

1. `模拟支付`
2. `模拟结算`

点击后只更新数据库状态，不调用真实微信支付。

### 9.3 为什么这样更适合你

1. 节省开发时间。
2. 避免个人主体支付限制问题。
3. 方便演示完整业务流程。
4. 老师更关注系统设计和实现，不一定要求真实支付。

---

## 10. Redis 和 OSS 用法

### 10.1 Redis 用法

Redis 只做两件事：

1. 登录状态缓存。
2. 绑定码缓存。

例如：

1. `login:token:{token}`
2. `bind:code:{elderlyUserId}`

### 10.2 OSS 用法

OSS 只做：

1. 用户头像上传。
2. 任务图片上传。

不做复杂私密文件管理。

### 10.3 配置示例

```yaml
spring:
  datasource:
    url: jdbc:postgresql://127.0.0.1:5432/yflg
    username: postgres
    password: 123456
    driver-class-name: org.postgresql.Driver
  redis:
    host: 127.0.0.1
    port: 6379
    database: 0

oss:
  endpoint: oss-cn-hangzhou.aliyuncs.com
  access-key-id: your-access-key-id
  access-key-secret: your-access-key-secret
  bucket-name: yflg-demo
  dir-prefix:
    avatar: avatar/
    task: task/
```

说明：

正式项目不建议把密钥直接写死，但展示项目文档里可以先这样写，后面再改成环境变量。

---

## 11. 接口简化设计

只做最核心接口即可。

### 11.1 登录接口

1. `POST /api/auth/wxLogin`
2. `POST /api/auth/logout`

### 11.2 用户接口

1. `GET /api/user/info`
2. `POST /api/user/save`

### 11.3 任务接口

1. `POST /api/task/add`
2. `GET /api/task/list`
3. `GET /api/task/detail`
4. `POST /api/task/grab`

说明：

1. `POST /api/task/add` 允许雇主和子女调用。
2. `POST /api/task/grab` 只允许老人调用。

### 11.4 订单接口

1. `GET /api/order/list`
2. `POST /api/order/finish`
3. `POST /api/order/cancel`

说明：

1. `GET /api/order/list` 建议按当前登录身份返回不同结果。
2. 老人查看自己接的订单。
3. 雇主查看自己发布任务产生的订单。
4. 子女查看自己发布任务产生的订单，以及绑定老人相关订单。
5. `POST /api/order/finish` 允许发单方操作，不允许老人操作。

### 11.5 子女绑定接口

1. `POST /api/bind/createCode`
2. `POST /api/bind/confirm`
3. `GET /api/bind/elderlyInfo`
4. `GET /api/bind/orderList`

### 11.6 留言接口

1. `POST /api/message/send`
2. `GET /api/message/list`

---

## 12. 开发计划

按照 20 天倒排，建议如下。

### 第 1 阶段：需求和数据库，2 天

1. 明确页面和模块。
2. 设计数据库表。
3. 画页面原型图。

### 第 2 阶段：后端基础，5 天

1. 搭建 Spring Boot 项目。
2. 配置 PostgreSQL、Redis、OSS。
3. 完成登录、用户、任务、订单、绑定接口。

### 第 3 阶段：小程序开发，7 天

1. 完成登录页。
2. 完成任务列表、详情、发布页面。
3. 完成订单页。
4. 完成子女绑定和留言页。

### 第 4 阶段：联调和优化，4 天

1. 前后端联调。
2. 修复 Bug。
3. 完善演示数据。

### 第 5 阶段：答辩准备，2 天

1. 准备 PPT。
2. 准备演示账号。
3. 准备演示流程。

---

## 13. 演示建议

老师演示时建议按这个流程走：

1. 雇主登录并发布任务。
2. 老人登录查看任务并接单。
3. 系统生成订单。
4. 子女登录并绑定老人。
5. 子女查看老人当前订单。
6. 子女给老人留言。
7. 雇主点击任务完成。
8. 订单状态变成已完成。

这样流程完整，而且功能不多，最容易讲清楚。

---

## 14. 本项目不做的内容

为了保证进度，以下内容建议全部不做：

1. 真实微信支付。
2. 真实提现。
3. 复杂风控。
4. 实名认证对接。
5. 地图 3 公里精准推荐。
6. 语音播报。
7. 保险服务。
8. 投诉申诉系统。
9. 复杂后台权限系统。

---

## 15. 结论

对于你现在这个项目，最合理的做法是做一个“简化展示版银发零工小程序”。

必须保留的核心内容是：

1. 任务发布。
2. 老人接单。
3. 订单状态流转。
4. 子女绑定。
5. 查看老人接单情况。
6. 留言沟通。

支付、提现这类功能不建议做真实功能，直接改为模拟状态展示即可，这样最适合你的时间和个人主体条件。

如果你需要，我下一步可以继续直接帮你补：

1. 这 7 张表的 PostgreSQL 建表 SQL。
2. Spring Boot 项目目录结构。
3. 微信小程序页面结构。
4. 答辩用的项目介绍稿。
