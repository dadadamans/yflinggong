# 银发零工网页端开发文档

## 1. 项目定位

本项目为一个 20 天内完成的网页端简化版项目，既要满足课程设计、比赛答辩或项目演示，也要具备基础上线部署能力，方便在云服务器上进行实际访问。

项目核心流程只保留：

1. 发单方发布任务。
2. 老人浏览任务并接单。
3. 生成订单并修改状态。
4. 子女绑定后查看老人接单情况并进行沟通。

说明：

1. 发单方包括雇主和子女。
2. 老人只负责接单，不负责发单。
3. 子女除绑定和沟通外，也可以替家庭发布任务需求。

本项目采用简化开发方案：

1. 后端使用 `JDK 17 + Spring Boot + PostgreSQL`。
2. 前端使用 `Vue 3` 开发网页端。
3. 文件上传使用后端本地目录存储。
4. 不做真实支付、真实提现，只做“模拟结算”。
5. 项目需要支持 `Nginx + Docker` 部署上线。

---

## 2. 为什么改成网页端

当前将项目改成网页端，有几个现实优势：

1. 网页端开发和调试更直接，适合赶工期。
2. 不依赖应用商店或平台审核流程，演示准备更灵活。
3. Vue 生态成熟，页面开发速度快。
4. 适合同时提供老人端、雇主端、子女端、后台管理端。
5. 更方便在答辩时通过浏览器展示完整业务流程。

所以本项目应以“能快速完成、能稳定演示、可部署上线、页面结构清晰”为第一目标，而不是做成完整商业平台。

---

## 3. 项目最终范围

### 3.1 用户角色

本项目只保留 4 个角色：

1. 老人用户。
2. 雇主用户。
3. 子女用户。
4. 管理员。

### 3.2 核心模块

本项目只做 5 个模块。

#### 模块一：登录注册模块

功能：

1. 网页端登录。
2. 用户选择身份：老人 / 雇主 / 子女。
3. 填写基础资料。

建议：

1. 演示项目不做复杂注册校验。
2. 可采用“手机号 + 验证码”或“账号密码 + 角色选择”的简化方案。
3. 如果后端时间紧，也可以直接使用演示账号登录。

#### 模块二：任务模块

功能：

1. 雇主发布任务。
2. 子女发布任务。
3. 老人查看任务列表。
4. 老人查看任务详情。
5. 老人接单。

当前规则补充：

1. 老人任务大厅只展示 `waiting` 状态任务。
2. 老人任务大厅会排除已绑定子女发布的待接单任务。
3. 老人申请接单前，体检报告必须已审核通过。

#### 模块三：订单模块

功能：

1. 接单后生成订单。
2. 订单状态展示。
3. 发单方点击“完成任务”。
4. 老人查看完成状态。

建议订单状态只保留：

1. 待接单。
2. 进行中。
3. 已完成。
4. 已取消。

当前代码实际状态为：

1. `waiting`
2. `applying`
3. `working`
4. `pending_payment`
5. `done`
6. `cancelled`

#### 模块四：子女绑定模块

这是本项目的重要特色模块，可以保留。

功能：

1. 老人发起绑定子女。
2. 子女输入绑定码进行绑定。
3. 子女查看老人资料。
4. 子女查看老人当前接单情况。
5. 子女查看订单状态。
6. 子女通过留言功能与老人沟通。

当前规则补充：

1. 子女注册时需要携带绑定码，注册和绑定在后端同一事务中完成。
2. 当前版本不开放解绑功能，解绑接口会直接返回业务错误。

#### 模块五：后台管理模块

后台只做简单管理：

1. 查看用户列表。
2. 查看任务列表。
3. 查看订单列表。
4. 查看子女绑定关系。

---

## 4. 技术方案

### 4.1 后端技术

| 技术 | 版本 |
|---|---|
| JDK | 17 |
| Spring Boot | 3.3.x |
| PostgreSQL | 16.x |
| Maven | 3.9+ |
| MyBatis-Plus | 3.5.x |

### 4.2 前端技术

| 技术 | 说明 |
|---|---|
| Vue 3 | 前端主框架 |
| Vite | 构建工具 |
| Vue Router | 路由管理 |
| Pinia | 状态管理 |
| Axios | 接口请求 |
| Element Plus | PC 端组件库 |
| ECharts | 数据图表，可选 |
| Nginx | 前端静态资源部署、反向代理 |
| Docker | 前后端容器化部署 |
| Docker Compose | 本地和服务器编排，可选 |

### 4.3 架构说明

本项目不需要微服务，直接做单体项目即可，但需要从一开始考虑部署结构。

系统结构：

```text
浏览器
   │
   ▼
Nginx
   ├─ 托管 Vue 3 前端静态文件
   └─ 反向代理 /api 到 Spring Boot
              │
              ▼
        Spring Boot 后端
           ├─ PostgreSQL
           └─ 本地上传目录
```

### 4.4 前端目录建议

```text
web/
├─ public/
├─ src/
│  ├─ api/
│  ├─ assets/
│  ├─ components/
│  ├─ layout/
│  ├─ router/
│  ├─ stores/
│  ├─ utils/
│  ├─ views/
│  │  ├─ login/
│  │  ├─ elderly/
│  │  ├─ employer/
│  │  ├─ child/
│  │  └─ admin/
│  ├─ App.vue
│  └─ main.js
├─ package.json
├─ vite.config.js
├─ Dockerfile
├─ nginx.conf
└─ .dockerignore
```

### 4.5 上线部署方案

为了保证项目最终可以访问，建议采用以下部署方式：

1. 前端 Vue 项目执行 `npm run build` 打包。
2. 使用 `Nginx` 托管 `dist/` 静态文件。
3. 通过 `Nginx` 将 `/api` 请求转发到 Spring Boot 后端。
4. 前后端分别构建 Docker 镜像。
5. 使用 `docker compose` 或单独 `docker run` 完成部署。

### 4.6 环境划分建议

建议至少区分两个环境：

1. 开发环境：Vite 本地启动，直接连接本机后端。
2. 生产环境：前端走 Nginx，接口统一访问相对路径 `/api`。

这样可以避免生产环境把后端 IP 和端口写死在前端代码中。

### 4.7 Nginx 作用说明

在本项目中，Nginx 主要承担 3 个职责：

1. 提供前端静态页面访问。
2. 反向代理后端接口，统一转发 `/api` 请求。
3. 为后续 HTTPS、域名访问、缓存控制提供入口。

### 4.8 Docker 作用说明

Docker 主要承担 3 个职责：

1. 统一前端运行环境，避免“本地能跑，服务器不能跑”。
2. 简化部署流程，方便迁移到云服务器。
3. 为后续答辩演示、项目交付和复现提供标准化部署方式。

### 4.9 当前实现规则补充

1. 登录成功后后端返回 Bearer token。
2. 当前 session 默认 `24` 小时过期，过期后需要重新登录。
3. 被禁用账号的旧 token 会失效。
4. 注册密码当前要求至少 `6` 位。
5. 体检报告当前只支持 `PDF` 上传。
6. 体检报告上传目录由后端配置项控制，不建议在代码中写死路径。
7. 评论中的被评价人姓名由后端根据真实任务参与方生成，不信任前端传值。
8. 任务、订单、用户等高频列表接口已支持分页参数，部分接口已切换为数据库级分页。

---

## 5. 页面设计

### 5.1 老人端页面

1. 登录页。
2. 首页。
3. 任务列表页。
4. 任务详情页。
5. 我的订单页。
6. 个人中心页。
7. 子女绑定页。
8. 留言查看页。

### 5.2 雇主端页面

1. 登录页。
2. 首页。
3. 发布任务页。
4. 我的任务页。
5. 订单管理页。
6. 个人中心页。

### 5.3 子女端页面

1. 登录页。
2. 绑定老人页。
3. 发布任务页。
4. 老人资料查看页。
5. 老人订单查看页。
6. 留言沟通页。
7. 个人中心页。

### 5.4 管理后台页面

1. 登录页。
2. 用户管理页。
3. 任务管理页。
4. 订单管理页。
5. 绑定关系管理页。

### 5.5 适老化要求

因为项目面向老人，页面要简单：

1. 字体大。
2. 按钮大。
3. 操作步骤少。
4. 颜色对比明显。
5. 首屏入口不要太多。
6. 重要按钮尽量固定位置。
7. 页面信息尽量卡片化展示。

### 5.6 响应式要求

虽然本项目是网页端，但建议兼容常见屏幕尺寸：

1. `>= 1200px` 适配后台和 PC 演示。
2. `768px - 1199px` 适配平板。
3. `< 768px` 适配手机浏览器。

建议：

1. 老人端和子女端页面可采用偏移动端布局。
2. 后台管理端采用标准 PC 管理台布局。

### 6.6 当前联调规则补充

1. 注册、登录、发布任务、绑定确认、留言发送、评价提交、资料保存、字体设置等核心请求已接入标准参数校验。
2. 非法参数当前统一返回 `400` 和明确错误信息。
3. `GET /api/task/list`、`GET /api/task/orders`、`GET /api/task/admin/waiting`、`GET /api/user/list` 已支持分页参数 `page`、`pageSize`。
4. 不传分页参数时，接口保持原有数组返回，兼容旧页面。

---

## 6. Vue 页面开发建议

### 6.1 路由设计建议

```text
/login
/elderly/home
/elderly/tasks
/elderly/task/:id
/elderly/orders
/elderly/profile
/elderly/bind
/elderly/messages

/employer/home
/employer/publish
/employer/tasks
/employer/orders
/employer/profile

/child/home
/child/bind
/child/publish
/child/elderly
/child/orders
/child/messages
/child/profile

/admin/home
/admin/users
/admin/tasks
/admin/orders
/admin/binds
```

### 6.2 布局建议

建议采用两套布局：

1. 老人端、雇主端、子女端使用业务端布局 `UserLayout`。
2. 后台管理使用管理端布局 `AdminLayout`。

例如：

1. 顶部显示系统名称和当前用户信息。
2. 左侧或底部显示功能导航。
3. 主区域展示列表、表单、详情页。

### 6.3 组件拆分建议

可以拆成以下通用组件：

1. `TaskCard` 任务卡片。
2. `OrderCard` 订单卡片。
3. `UserInfoPanel` 用户信息面板。
4. `BindCodeBox` 绑定码展示组件。
5. `MessageBoard` 留言板组件。
6. `StatusTag` 状态标签组件。

### 6.4 状态管理建议

Pinia 里建议至少保留 3 个 store：

1. `userStore` 保存用户信息、角色、token。
2. `taskStore` 保存任务列表筛选条件。
3. `bindStore` 保存绑定关系和当前老人信息。

### 6.5 权限控制建议

网页端需要做基础路由守卫：

1. 未登录不能进入业务页面。
2. 老人不能访问发布任务页。
3. 雇主不能访问子女绑定页。
4. 管理员只能访问后台模块。

---

## 7. 数据库简化设计

为了赶工期，数据库建议只做 7 张核心表。

### 7.1 用户表 `sys_user`

保存所有用户基础信息。

主要字段：

1. `id`
2. `username`
3. `password`
4. `nickname`
5. `avatar_url`
6. `mobile`
7. `role_type`
8. `created_at`

说明：

1. 如果你使用演示账号方案，可以直接保留 `username` 和 `password`。
2. 如果你想改成短信验证码登录，也可以弱化密码字段。

### 7.2 老人信息表 `elderly_profile`

主要字段：

1. `id`
2. `user_id`
3. `real_name`
4. `age`
5. `gender`
6. `health_desc`
7. `skill_tags`
8. `emergency_contact`
9. `emergency_mobile`

### 7.3 雇主信息表 `employer_profile`

主要字段：

1. `id`
2. `user_id`
3. `name`
4. `mobile`
5. `remark`

### 7.4 任务表 `task_info`

主要字段：

1. `id`
2. `publisher_user_id`
3. `publisher_role_type`
4. `task_type`
5. `title`
6. `content`
7. `address`
8. `start_time`
9. `salary`
10. `status`
11. `image_url`

说明：

1. `publisher_user_id` 表示任务发布人，可以是雇主，也可以是子女。
2. `publisher_role_type` 用于区分当前发布人身份，建议取值：`employer` / `child`。

### 7.5 订单表 `order_info`

主要字段：

1. `id`
2. `task_id`
3. `elderly_user_id`
4. `employer_user_id`
5. `publisher_role_type`
6. `status`
7. `create_time`
8. `finish_time`

说明：

1. 展示项目里可以继续保留 `employer_user_id` 这个字段名，但它在业务上应理解为“发单方用户 id”。
2. 如果你想在后端实现时命名更严谨，也可以改成 `publisher_user_id`。

### 7.6 子女绑定表 `child_bind`

主要字段：

1. `id`
2. `elderly_user_id`
3. `child_user_id`
4. `bind_code`
5. `bind_status`
6. `created_at`

### 7.7 留言表 `message_record`

用于老人和子女沟通。

主要字段：

1. `id`
2. `elderly_user_id`
3. `child_user_id`
4. `sender_user_id`
5. `content`
6. `created_at`

---

## 8. 子女绑定模块设计

这个模块建议做成你项目的亮点。

### 8.1 业务流程

#### 方案一：老人生成绑定码，子女输入绑定码

流程如下：

1. 老人登录后进入“子女绑定”页面。
2. 系统生成一个 6 位绑定码。
3. 子女登录后输入绑定码。
4. 系统校验成功后建立绑定关系。

这个方案最简单，最适合当前项目。

### 8.2 子女可查看内容

绑定成功后，子女可以查看：

1. 老人姓名。
2. 老人技能标签。
3. 老人当前接单情况。
4. 老人订单状态。
5. 老人紧急联系人信息。

### 8.3 子女可执行操作

建议只做轻量功能，不要做复杂代操作。

子女端可保留：

1. 查看老人订单。
2. 给老人留言。
3. 查看任务是否已完成。
4. 替家庭发布任务需求。

不要做：

1. 代接单。
2. 代提现。
3. 修改老人实名信息。

### 8.4 沟通功能简化方案

不要做实时聊天，时间不够。

建议做“留言板”功能：

1. 子女可留言。
2. 老人可查看留言。
3. 老人也可以回复。

这样既能体现“沟通”，开发量也小很多。

---

## 9. 支付与提现处理建议

### 9.1 本项目建议

本项目不接真实支付，不接真实提现。

### 9.2 替代实现

你可以这样设计：

1. 发单方发布任务时填写报酬金额。
2. 老人完成任务后，订单状态改为“已完成”。
3. 系统显示“待结算”或“模拟已结算”。

如果你一定要保留展示效果，可以加两个按钮：

1. `模拟支付`
2. `模拟结算`

点击后只更新数据库状态，不调用真实支付接口。

### 9.3 为什么这样更适合当前项目

1. 节省开发时间。
2. 避免支付和结算接入复杂度。
3. 方便演示完整业务流程。
4. 老师更关注系统设计和实现，不一定要求真实支付。

---

## 10. 配置与上传说明

### 10.1 当前后端配置示例

```yaml
server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://127.0.0.1:5432/oldboss
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:287556}
    driver-class-name: org.postgresql.Driver
  sql:
    init:
      mode: never
```

说明：

1. 当前项目默认连接本地 `oldboss` 数据库。
2. 当前项目不会自动执行 `schema.sql` 和 `seed.sql`。
3. 上传文件当前保存到后端本地 `uploads/healthReports/` 目录。

### 10.2 生产环境建议

真正上线时，以下配置应通过环境变量或容器注入方式提供：

1. 数据库地址、账号、密码。
2. 后端服务端口。
3. 文件上传访问域名或静态文件挂载目录。

不要把这些生产配置直接写死在前端代码或公开仓库里。

---

## 11. 接口简化设计

只做最核心接口即可。

### 11.1 登录接口

1. `POST /api/auth/register`
1. `POST /api/auth/login`
2. `POST /api/auth/logout`
3. `GET /api/auth/currentUser`

说明：

1. 网页端使用普通账号密码登录。
2. 登录成功后返回 `token + 用户角色 + 基础信息`。

### 11.2 用户接口

1. `GET /api/user/info`
2. `POST /api/user/save`
3. `GET /api/user/list`
4. `POST /api/user/setEnabled`
5. `POST /api/user/setElderlyFontSize`

### 11.3 体检报告接口

1. `POST /api/health/upload`
2. `POST /api/health/review`
3. `GET /api/health/status`

说明：

1. 体检报告当前只支持 `PDF` 文件。
2. 文件名无效、无扩展名或类型不合法时，接口返回业务错误。

### 11.4 任务接口

1. `GET /api/task/categories`
2. `POST /api/task/add`
3. `GET /api/task/list`
4. `GET /api/task/detail?id={id}`
5. `POST /api/task/apply`
6. `POST /api/task/approve`
7. `POST /api/task/reject`
8. `GET /api/task/admin/waiting`

说明：

1. `POST /api/task/add` 允许雇主和子女调用。
2. `POST /api/task/apply` 只允许老人调用。
3. 审核接单由雇主或子女端完成。
4. 老人任务大厅只展示 `waiting` 状态任务。
5. 老人任务大厅会排除已绑定子女发布的待接单任务。
6. 老人申请接单前，体检报告必须已审核通过。

### 11.5 订单接口

1. `GET /api/task/orders`
2. `POST /api/task/order/finish`
3. `POST /api/task/order/pay`
4. `POST /api/task/order/cancel`
5. `POST /api/task/order/delete`

说明：

1. `GET /api/task/orders` 按当前登录身份返回不同结果。
2. 老人查看自己接的订单。
3. 雇主查看自己发布任务产生的订单。
4. 子女既可查看自己发布任务产生的订单，也可通过绑定接口查看老人相关订单。
5. `POST /api/task/order/finish` 由老人操作。
6. `POST /api/task/order/pay` 由雇主或子女操作。

### 11.6 子女绑定接口

1. `POST /api/bind/createCode`
2. `POST /api/bind/confirm`
3. `POST /api/bind/unbind`
   当前版本暂不支持解绑，前端不提供解绑入口。
4. `GET /api/bind/elderlyInfo`
5. `GET /api/bind/orderList`
6. `GET /api/bind/list`

### 11.7 留言接口

1. `GET /api/message/list`
2. `POST /api/message/send`

### 11.8 评价接口

1. `POST /api/comment/add`
2. `GET /api/comment/user/{userId}`
3. `GET /api/comment/user/{userId}/stats`
4. `GET /api/comment/task/{taskId}`

说明：

1. 当前只允许在任务状态为 `done` 时评价。
2. 只有任务参与方可以评价任务另一方。
3. 不能评价自己。

### 11.9 管理统计接口

1. `GET /api/admin/stats`

---

## 12. 前端开发落地建议

### 12.1 页面实现建议

如果使用 Vue 3 + Element Plus，页面可以这样组织：

1. 列表页使用 `el-table` 或卡片列表。
2. 表单页使用 `el-form`。
3. 详情页使用描述列表和状态标签。
4. 适老页面可适当减少表格，更多使用大卡片和大按钮。

### 12.2 接口封装建议

建议在 `src/api/` 下按模块拆分：

1. `auth.js`
2. `user.js`
3. `task.js`
4. `order.js`
5. `bind.js`
6. `message.js`
7. `comment.js`
8. `admin.js`

### 12.3 统一请求处理

建议封装一个统一的 `request.js`：

1. 自动携带 token。
2. 统一处理未登录状态。
3. 统一处理错误提示。
4. 统一处理接口超时。

### 12.4 环境变量建议

前端建议至少保留以下环境变量：

1. `VITE_API_BASE_URL`
2. `VITE_APP_TITLE`

建议：

1. 开发环境使用完整地址，例如 `http://127.0.0.1:8080`。
2. 生产环境使用 `/api` 相对路径，由 Nginx 转发。

### 12.5 前端上线要求

为了便于上线，前端工程建议补充以下文件：

1. `Dockerfile`
2. `nginx.conf`
3. `.dockerignore`
4. `.env.example`

这样既能本地开发，也能直接构建生产镜像。

### 12.6 演示数据建议

为了保证答辩效果，建议准备三类演示账号：

1. 老人账号。
2. 雇主账号。
3. 子女账号。

管理员账号可选做。

---

## 13. 上线部署设计

### 13.1 前端 Dockerfile 思路

前端推荐使用多阶段构建：

1. 第一阶段使用 `Node.js` 安装依赖并执行 `npm run build`。
2. 第二阶段使用 `Nginx` 托管打包产物。

优点：

1. 镜像更小。
2. 部署更稳定。
3. 不需要在生产环境安装 Node.js。

### 13.2 Nginx 配置建议

Nginx 建议实现以下能力：

1. `/` 指向前端静态文件目录。
2. `try_files` 处理 Vue Router history 路由刷新问题。
3. `/api/` 转发给 Spring Boot 服务。
4. 设置基础缓存头。

参考逻辑如下：

```nginx
server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    location /api/ {
        proxy_pass http://backend:8080/api/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
```

### 13.3 Docker Compose 建议

如果你希望本地和服务器统一管理，可以采用如下容器划分：

1. `frontend`：Vue + Nginx
2. `backend`：Spring Boot
3. `db`：PostgreSQL

系统结构：

```text
Internet
   │
   ▼
frontend(Nginx)
   │
   ▼
backend(Spring Boot)
   └─ db
```

### 13.4 域名和 HTTPS 建议

如果项目需要公网访问，建议：

1. 购买云服务器并开放 `80`、`443` 端口。
2. 域名解析到服务器公网 IP。
3. 使用 Nginx 配置 HTTPS 证书。
4. 所有 API 和页面统一走 HTTPS。

### 13.5 服务器部署流程建议

一个基础上线流程如下：

1. 在服务器安装 Docker 和 Docker Compose。
2. 拉取项目代码。
3. 配置后端环境变量。
4. 构建前后端镜像。
5. 启动 PostgreSQL、后端、前端容器。
6. 通过域名访问并进行联调验证。

### 13.6 上线后检查项

上线后至少检查以下内容：

1. 登录是否正常。
2. 发布任务是否成功。
3. 老人接单是否生成订单。
4. 子女绑定是否成功。
5. 留言发送是否正常。
6. Nginx 刷新页面时是否出现 404。
7. 上传图片是否能正常访问。

---

## 14. 开发计划

按照 20 天倒排，建议如下。

### 第 1 阶段：需求和数据库，2 天

1. 明确页面和模块。
2. 设计数据库表。
3. 画页面原型图。

### 第 2 阶段：后端基础，5 天

1. 搭建 Spring Boot 项目。
2. 配置 PostgreSQL 和基础上传目录。
3. 完成登录、用户、任务、订单、绑定接口。

### 第 3 阶段：Vue 网页端开发，7 天

1. 搭建 Vue 3 + Vite 项目。
2. 完成登录页和基础布局。
3. 完成任务列表、详情、发布页面。
4. 完成订单页。
5. 完成子女绑定和留言页。
6. 完成后台基础管理页。

### 第 4 阶段：联调和优化，4 天

1. 前后端联调。
2. 修复 Bug。
3. 完善演示数据。
4. 优化适老化展示效果。

### 第 5 阶段：部署和答辩准备，2 天

1. 编写 Dockerfile 和 Nginx 配置。
2. 准备服务器部署脚本。
3. 准备 PPT。
4. 准备演示账号。
5. 准备演示流程。

---

## 15. 演示建议

老师演示时建议按这个流程走：

1. 雇主登录并发布任务。
2. 老人登录查看任务并接单。
3. 系统生成订单。
4. 子女登录并绑定老人。
5. 子女查看老人当前订单。
6. 子女给老人留言。
7. 雇主点击任务完成。
8. 订单状态变成已完成。

这样流程完整，而且功能不多，最容易讲清楚。

如果是网页端演示，建议准备：

1. PC 浏览器演示管理端和雇主端。
2. 浏览器响应式模式演示老人端和子女端。

---

## 16. 本项目不做的内容

为了保证进度，以下内容建议全部不做：

1. 真实支付。
2. 真实提现。
3. 复杂风控。
4. 实名认证对接。
5. 地图 3 公里精准推荐。
6. 实时聊天。
7. 保险服务。
8. 投诉申诉系统。
9. 复杂后台权限系统。

---

## 17. 结论

对于当前这个项目，最合理的做法是做一个“简化但可上线的银发零工网页系统”。

必须保留的核心内容是：

1. 任务发布。
2. 老人接单。
3. 订单状态流转。
4. 子女绑定。
5. 查看老人接单情况。
6. 留言沟通。

前端建议明确采用 `Vue 3 + Vite + Vue Router + Pinia + Element Plus + Nginx + Docker`，这样既适合在短周期内完成网页端页面开发，也便于后续部署到服务器并进行实际访问。
